[package]
name = "ergo-sync"
version = "0.1.0"
edition = "2021"
description = "Chain sync state machine for the Ergo Rust node"

[dependencies]
enr-p2p = { path = "../p2p" }
enr-chain = { path = "../chain" }
tokio = { version = "1", features = ["time", "sync"] }
tracing = "0.1"
